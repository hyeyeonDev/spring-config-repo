server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hyeyeonDev/spring-config-repo.git
          clone-on-start: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
